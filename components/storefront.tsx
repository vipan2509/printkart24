'use client'

import Link from 'next/link'
import { Heart, Menu, MessageCircle, Search, ShoppingBag, Star, X } from 'lucide-react'
import { useState } from 'react'
import type { Product } from '@/lib/catalog'
import { formatPrice } from '@/lib/catalog'
import { useCart, useWishlist } from '@/hooks/use-store'

export function Header() { const [open, setOpen] = useState(false); const { count } = useCart(); return <><div className="utility-bar">Made for your next big idea. <span>Free delivery on orders over ₹999</span></div><header className="site-header"><div className="page-shell header-row"><button className="icon-button mobile-only" onClick={() => setOpen(true)} aria-label="Open menu"><Menu size={21}/></button><Link href="/" className="brand-mark"><span>PRINT</span><i>KART</i><b>®</b></Link><form action="/search" className="search-box header-search"><Search size={18}/><input name="q" placeholder="Search products, gifts & ideas" aria-label="Search products"/><kbd>⌘ K</kbd></form><nav className="desktop-nav"><Link href="/category/business-printing">Business printing</Link><Link href="/category/photo-gifts">Photo gifts</Link><Link href="/category/clothing">Clothing</Link><Link href="/category/stationery">Stationery</Link></nav><div className="header-actions"><Link href="/search" className="icon-button mobile-search" aria-label="Search"><Search size={20}/></Link><Link href="/wishlist" className="icon-button" aria-label="Wishlist"><Heart size={20}/></Link><Link href="/cart" className="bag-button" aria-label={`Cart with ${count} items`}><ShoppingBag size={20}/><span>{count}</span></Link></div></div></header>{open && <div className="mobile-drawer"><div className="drawer-scrim" onClick={() => setOpen(false)}/><aside><div className="drawer-top"><span className="brand-mark"><span>PRINT</span><i>KART</i><b>®</b></span><button className="icon-button" onClick={() => setOpen(false)} aria-label="Close menu"><X size={21}/></button></div><nav><Link href="/" onClick={() => setOpen(false)}>Home</Link><Link href="/category/business-printing" onClick={() => setOpen(false)}>Business printing</Link><Link href="/category/photo-gifts" onClick={() => setOpen(false)}>Photo gifts</Link><Link href="/category/clothing" onClick={() => setOpen(false)}>Clothing</Link><Link href="/category/stationery" onClick={() => setOpen(false)}>Stationery</Link></nav></aside></div>}</> }

export function Footer() { return <footer className="footer"><div className="page-shell"><div className="footer-top"><div><div className="brand-mark footer-brand"><span>PRINT</span><i>KART</i><b>®</b></div><p className="footer-intro">Thoughtful print for<br/>every kind of idea.</p></div><div className="newsletter"><strong>Get the good stuff in your inbox.</strong><div className="newsletter-input"><input aria-label="Email address" placeholder="Your email address"/><button>→</button></div><small>Fresh ideas, no inbox clutter.</small></div></div><div className="footer-links"><div><strong>Shop</strong><Link href="/category/business-printing">Business printing</Link><Link href="/category/photo-gifts">Photo gifts</Link><Link href="/category/clothing">Clothing</Link><Link href="/category/stationery">Stationery</Link></div><div><strong>Help</strong><Link href="/contact">Contact us</Link><Link href="/faq">FAQs</Link><Link href="/orders">Track your order</Link><Link href="/shipping">Shipping & delivery</Link></div><div><strong>About</strong><Link href="/about">Our story</Link><Link href="/account">My account</Link><Link href="/wishlist">Wishlist</Link></div></div><div className="footer-bottom"><span>© 2026 PRINTKART. Made with intent.</span><span>Privacy · Terms · India</span></div></div></footer> }

export function WhatsAppButton() { const href = 'https://wa.me/918284001860?text=Hi%20PRINTKART%2C%20I%20need%20help%20with%20my%20order.'; return <a className="whatsapp-fab" href={href} target="_blank" rel="noreferrer" aria-label="Chat with PRINTKART on WhatsApp"><MessageCircle size={20} fill="currentColor" /><span>Chat with us</span></a> }

export function Rating({ rating, reviews }: { rating: number; reviews: number }) { return <span className="rating"><Star size={14} fill="currentColor"/> {rating} <small>({reviews})</small></span> }

export function ProductCard({ product }: { product: Product }) { const [wishlist, setWishlist] = useWishlist(); const saved = wishlist.includes(product.slug); return <article className="catalog-card"><Link href={`/product/${product.slug}`} className="catalog-image"><img src={product.image} alt={product.name}/>{product.badge && <span className="product-badge">{product.badge}</span>}<button type="button" className={`favorite-button ${saved ? 'is-favorite' : ''}`} aria-label={`Save ${product.name}`} onClick={(event) => { event.preventDefault(); setWishlist(saved ? wishlist.filter((item) => item !== product.slug) : [...wishlist, product.slug]) }}><Heart size={18} fill={saved ? 'currentColor' : 'none'}/></button></Link><div className="catalog-info"><Link href={`/product/${product.slug}`}><h3>{product.name}</h3></Link><Rating rating={product.rating} reviews={product.reviews}/><div><strong>{formatPrice(product.price)}</strong>{product.originalPrice && <del>{formatPrice(product.originalPrice)}</del>}<small> onwards</small></div></div></article> }

export function ProductGrid({ products }: { products: Product[] }) { return <div className="catalog-grid">{products.map((product) => <ProductCard product={product} key={product.slug}/>)}</div> }
